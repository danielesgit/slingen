[string]

# Lib name
libname = Polly-3.9

# Prefix of the tester to be called
testerprefix = polly

legendname = Polly-3.9

cc = $$POLLYROOT/clang++

# CFLAGS variable to pass to the compiler
cflags-vec = -O3 -mllvm -polly -mllvm -polly-vectorizer=stripmine


[code]

# Marker for the plot
marker = 's'
color = '#404040' 
lw = 2.

