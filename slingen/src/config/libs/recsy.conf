[string]

# Lib name
libname = RECSY

# Prefix of the tester to be called
testerprefix = recsy

legendname = RECSY


[code]


ldflags = ' '.join([opts.get('ldflags', ''), '-mkl -lifcore $$RECSYROOT/librecsy.a $$SLICOTROOT/slicot.a']).strip()

# Marker for the plot
marker = '*'
color = '#909090' 
lw = 2.

