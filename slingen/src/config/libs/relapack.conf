[string]

# Lib name
libname = ReLAPACK

# Prefix of the tester to be called
testerprefix = relapack

legendname = ReLAPACK


[code]

ldflags = ' '.join([opts.get('ldflags', ''), ' $$RELAPACKROOT/librelapack.a -mkl -lifcore']).strip()

# Marker for the plot
marker = 'p'
color = '#707070' 
lw = 2.
